<form method="post" action="{{ path('app_contrat_pret_delete', {'id': contrat_pret.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat_pret.id) }}">
    <button class="btn">Delete</button>
</form>
