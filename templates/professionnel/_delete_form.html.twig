<form method="post" action="{{ path('app_professionnel_delete', {'id': professionnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professionnel.id) }}">
    <button class="btn">Delete</button>
</form>
