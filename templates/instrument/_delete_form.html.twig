<form method="post" action="{{ path('app_instrument_delete', {'id': instrument.id}) }}" onsubmit="return confirm('Vous êtes sûr de vouloir supprimer cet instrument ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instrument.id) }}">
    <button class="btn">Delete</button>
</form>
